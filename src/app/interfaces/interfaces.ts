export interface Card {
    id: number;
    brand: string;
    model: string;
    description: string;
    image: string;
}

export interface Button {
    title: string
    link: string
    icon: string
}